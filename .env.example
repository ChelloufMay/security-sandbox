# Top-level .env.example - developer notes
# Copy this file to .env files in the backend/frontend directories as required.
# DO NOT put real secrets here. Replace placeholder values when running locally.

# Backend (copy this section to backend/.env or backend/.env.local)
DJANGO_DEBUG=1
DJANGO_SECRET_KEY=replace-this-with-a-random-secret
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1
EMAIL_HOST=mailhog
EMAIL_PORT=1025
DATABASE_URL=postgres://postgres:password.@host.docker.internal:5432/security_sandbox_db
DJANGO_SUPERUSER_USERNAME=admin
DJANGO_SUPERUSER_EMAIL=admin@example.com
DJANGO_SUPERUSER_PASSWORD=changeme123

# Frontend / Vite (example, copy to frontend/.env or frontend/.env.local)
# Vite requires VITE_ prefix for environment variables available in the browser
VITE_API_URL=http://localhost:8000
