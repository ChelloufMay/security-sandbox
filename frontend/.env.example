# frontend/.env.example
# ---------------------------
# Template for frontend environment variables (commit this file).
# Copy to .env locally and adjust values.
# ---------------------------

# API base URL the frontend will call
VITE_API_URL=http://localhost:8000/api

# Frontend base URL
VITE_FRONTEND_URL=http://localhost:5173

# Enable the dev inbox (simulated email/SMS UI) — set to "true" or "false"
VITE_FEATURE_DEV_INBOX=true

# NOTE: Never put private keys or secrets here. Client env vars are public.
