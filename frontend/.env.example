# frontend/.env.example
# Template env file. Copy to .env.local and customize for your machine.
#
# Use VITE_API_BASE='/' when running Vite with proxy.
# Use VITE_API_BASE='http://localhost:8000' to call backend directly (you'll need to adjust backend CORS).

VITE_API_BASE=/
# VITE_BACKEND_URL=http://localhost:8000
VITE_MAILHOG_URL=http://localhost:8025
VITE_APP_TITLE="Security Sandbox (Frontend)"

# Optional dev toggles
# VITE_ENABLE_DEMO_VAULT=true

# If you plan to call third-party dev APIs you may add keys here (dev-only)
# VITE_SOME_API_KEY=
